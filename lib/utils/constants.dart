import 'package:flutter/cupertino.dart';
import 'package:haflaway/utils/dimensions.dart';

const evPageSize = 10;
const atsPageSize = 20;

const invmlength = 800;
const remmlength = 480;
const opacgen = 0.08;
const bdrWidthGen = 0.5;

EdgeInsets paddPrime = EdgeInsets.only(
  top: psm * 10,
  left: psm,
  right: psm,
  bottom: psm,
);
